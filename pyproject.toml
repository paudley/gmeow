[project]
name = "gmeow"
version = "1.0.0"
description = "Local Gmail intelligence server for agents, MCP, and REST automation"
readme = "README.md"
requires-python = ">=3.13"
license = "MIT"
authors = [
    { name = "Blackcat Informatics Inc." },
]
keywords = ["gmail", "mcp", "email", "agents", "archive", "semantic-search", "knowledge-graph"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Framework :: FastAPI",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
    "Topic :: Communications :: Email",
    "Topic :: Database",
    "Topic :: Internet :: WWW/HTTP :: HTTP Servers",
]
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.10",
    "google-api-python-client>=2.150",
    "google-auth>=2.36",
    "google-auth-httplib2>=0.2",
    "rustworkx>=0.15",
    "mcp>=1.8",
    "semchunk>=4.0.0",
    "spacy>=3.8.14",
    "scikit-learn>=1.8.0",
    "psycopg[binary]>=3.2",
    "pgvector>=0.3",
    "blake3>=1.0",
    "zstandard>=0.23",
    "tantivy>=0.22",
    "alembic>=1.18.4",
    "en-core-web-sm",
]

[project.optional-dependencies]
test = [
    "pytest>=8.3",
    "httpx>=0.27",
]

[project.scripts]
gmeow = "gmeow.cli:main"

[project.urls]
Homepage = "https://github.com/blackcat-informatics/gmeow"
Repository = "https://github.com/blackcat-informatics/gmeow"
Issues = "https://github.com/blackcat-informatics/gmeow/issues"
PyPI = "https://pypi.org/project/gmeow/"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gmeow"]

[tool.hatch.build.targets.sdist]
only-include = [
    "alembic.ini",
    "CODE_OF_CONDUCT.md",
    "config.toml-example",
    "CONTRIBUTING.md",
    "docs",
    "LICENSE",
    "main.py",
    "migrations",
    "pyproject.toml",
    "README.md",
    "scripts",
    "SECURITY.md",
    "src",
    "tests",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
norecursedirs = [".git", ".venv", "coding-ethos", "data", "dist", "vendor"]

[tool.uv.sources]
en-core-web-sm = { url = "https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl" }
