# Python Package Tests

These tests validate the Phase 00 `gmeow-intel` package skeleton. They focus on schema behavior
and importability, not worker runtime execution.

Runtime analyzer tests should be added with the ANALYSIS phase when queue consumption, dispatch,
and result persistence are implemented.
